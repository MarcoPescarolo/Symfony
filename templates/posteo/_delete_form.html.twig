<form method="post" action="{{ path('app_posteo_delete', {'id': posteo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ posteo.id) }}">
    <button class="btn">Delete</button>
</form>
