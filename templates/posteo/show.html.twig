{% extends 'base.html.twig' %}

{% block title %}Posteo{% endblock %}

{% block body %}
    <h1>Posteo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ posteo.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ posteo.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ posteo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_posteo_index') }}">back to list</a>

    <a href="{{ path('app_posteo_edit', {'id': posteo.id}) }}">edit</a>

    {{ include('posteo/_delete_form.html.twig') }}
{% endblock %}
