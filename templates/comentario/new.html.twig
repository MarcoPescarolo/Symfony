{% extends 'base.html.twig' %}

{% block title %}New Comentario for {{ posteo.titulo }}{% endblock %}

{% block body %}
    <h1>Create new Comentario for "{{ posteo.titulo }}"</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_posteo_index') }}">back to list</a>
{% endblock %}